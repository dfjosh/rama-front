{{#if session.isAuthenticated}}
  {{#if current.user.isAdmin}}
    {{admin-toolbar-comp}}
    {{post-modal-comp headerText="New Post" modalId="newPostModal"}} {{!-- IMPORTANT needs to live here because the admin-toolbar-comp is positioned fixed which bootstrap modals don't like --}}
    {{episode-modal-comp headerText="New Episode" modalId="newEpisodeModal"}}
  {{/if}}
{{/if}}

{{outlet}}