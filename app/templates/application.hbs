{{#if session.isAuthenticated}}
  {{admin-toolbar-comp}}
  {{post-modal-comp model=newPost modalId="newPostModal"}} {{!-- IMPORTANT needs to live here because the admin-toolbar-comp is positioned fixed which bootstrap modals don't like --}}
{{/if}}

{{outlet}}